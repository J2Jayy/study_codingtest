"""

이중for문만 가능.
배열의 모든 요소에 대해
i == 배열 최댓값 하면서 하나씩 줄여가고 (첫 for문)
이제 탐색. for문이용해서 배열 모든 요소 가 모든 가능한 방향8개에 대해 위  불가능하면 컨티뉴
아니면 첫 반복문 줄역가ㅣ.

"""
N , M = map(int,input().split())
collum = N
square = [list(map(int,input(i))) for i in range(N)]

if M <= N :
    collum = M


for i in range(collum,0,-1):
    for j in range(0,N):
        for k in range(0,M):
            if square[j][k] < 0


    


